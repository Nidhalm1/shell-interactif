#include <stdlib.h>
#include <string.h>

char *parse_command(char *line)
{
    // Extrait la première commande, à compléter pour une analyse plus poussée
    return strdup(line); // Pour l'instant, retourne une copie de la ligne
}
